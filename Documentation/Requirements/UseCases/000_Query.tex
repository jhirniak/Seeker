\documentclass[12pt, a4paper]{usecase}

\setid{0}
\setname{Database query}
\setDatecCreated{26 May 2014}
\setDateLastUpdated{26 May 2014}

\setActors{Consumer}
\setDescription{Actor wants to extract information from the database relating one or multiple documents that were present on source listed web pages.}
\setPreconditions{Actor on the query page (being also the landing page)}
\setPostconditions{The result is displayed in the browser and can be retrieved in prefered format (pdf, txt), the search refined, or modified and then retrieved.}
\setPriority{100}
\setFrequency{100}
\setBasicFlow{Go to the search page, {Insert query in the prefered format (library index, query language, etc.}), Refine search }
\setAlternativeFlow{Flow Alt}
\setExceptions{}
\setSpecialRequirements{}
\setAssumptions{}
\setNotes{}


\begin{document}

\pagenumbering{gobble}
\topskip0pt
\vspace*{\fill}

\begin{usecase}	
\end{usecase}

\vspace*{\fill}

\end{document}